# Phase-0 bring-up config for the Pi Zero 2 W.
#
# References (see docs/REAL_HW_BRINGUP.md for the verified-facts section):
#   - raspberrypi.com/documentation/computers/config_txt.html
#   - raspberrypi/firmware/boot/overlays/README (master)
#   - raspberrypi/tools/armstubs/armstub8.S
#
# Trimmed against a working Raspbian Bookworm boot on the same
# Pi Zero 2 W + HDMI panel (see commit message for the dump). Removed
# settings that didn't address the firmware-stage flicker / white-bar:
#   hdmi_drive=2  hdmi_force_hotplug=1  config_hdmi_boost=7
#   hdmi_group=1  hdmi_mode=4           avoid_warnings=1
# Raspbian shows the same flicker until KMS does a clean modeset
# partway through systemd boot, so it isn't a config-knob fix; the
# remediation lives in our own re-modeset path (or its absence).

# 64-bit kernel. Selects the AArch64 armstub which eret's to EL2h before
# branching to kernel8.img at 0x80000. Without this we land at EL3 or
# the 32-bit armstub.
arm_64bit=1

# Use the cut-down GPU firmware (start_cd.elf + fixup_cd.dat).
# Per the config.txt docs: "Cut-down firmware (start*cd.elf and
# fixup*cd.dat) cannot be selected this way [via start_file=] - the
# system will fail to boot. The only way to enable the cut-down
# firmware is to specify gpu_mem=16." Any other value selects the
# full pair (start.elf + fixup.dat) — build-sd.sh ships both pairs,
# so either setting boots.
#
# We tested the full firmware (gpu_mem=64) for the white-bar hunt:
# it did not clear the persistent top-row white bar, so landscape
# stays on the cut-down variant (see
# `host_io::pi_fb::FIRMWARE_TOP_BAR_PX` for the workaround).
# Portrait rotation is documented as needing more GPU memory than
# gpu_mem=16 and is verified working with gpu_mem=64 — set that
# together with the rotation pair below.
gpu_mem=16

# Suppress the firmware's rainbow boot splash. Our own boot splash
# takes over from the moment `display::splash::init` runs in kmain.
disable_splash=1

# The bootloader (nhboot/, built by scripts/build-sd.sh). The firmware
# loads it at 0x80000 and enters it at EL2; it relocates itself to
# 0x10000000, validates the hypervisor container loaded by the
# `initramfs` line below, copies the hypervisor payload to 0x80000 and
# enters it there — or, when scripts/pi-upload.py is talking on the
# serial cable, receives a new image first and writes it back to
# HYPERV.IMG. See docs/REAL_HW_BRINGUP.md, "Serial image upload".
kernel=kernel8.img

# The hypervisor image, as the fixed 16 MiB HYPERV.IMG container (4 KiB
# header with magic, length and CRC-32s, then the raw kernel image).
# `initramfs <file> <addr>` makes the firmware load an arbitrary file
# to a physical address — the same mechanism Linux uses for its
# initrd; per raspberrypi.com/documentation/computers/config_txt.html
# it "performs the actions of both ramfsfile and ramfsaddr", and it is
# the one config.txt option written without an `=`. The address is a
# contract with `nhboot/src/image.rs::IMAGE_ADDR`.
#
# RAM at nhboot entry:
#   0x00080000  nhboot as loaded (kernel8.img); later the hypervisor
#   0x02000000  HYPERV.IMG as loaded (this line)
#   0x03000000  staging for an image received over serial
#   0x10000000  nhboot after relocating itself out of the way
initramfs HYPERV.IMG 0x02000000

# Enable a serial console on GPIO 14/15 (header pins 8 and 10).
enable_uart=1

# Also enable firmware-side UART logging from bootcode.bin and start.elf.
# Useful as an early checkpoint: if no bytes appear on the wire at all,
# the problem is upstream of our kernel (SD layout, GPIO ALT, baud).
uart_2ndstage=1

# CRITICAL: PL011 (UART0) is wired to the onboard Bluetooth chip by
# default on the Pi Zero 2 W; the GPIO header carries the mini-UART
# (UART1) instead. Our code drives PL011 at 0x3F20_1000, so we route it
# to GPIO 14/15 by disabling BT. Quote from raspberrypi/firmware
# overlays/README: "Disable onboard Bluetooth on Bluetooth-capable
# Raspberry Pis. On Pis prior to Pi 5 this restores UART0/ttyAMA0 over
# GPIOs 14 & 15."
dtoverlay=disable-bt

# Disable HDMI overscan padding. Matches Raspbian. Some monitors trim
# a fixed margin assuming consumer-TV overscan; on monitors with no
# real overscan that margin is just lost pixels.
disable_overscan=1

# Probe HDMI EDID and pick the panel's preferred mode. Matches the
# Raspbian default; replaces the explicit `hdmi_group=1 hdmi_mode=4`
# (CEA 720p60) we used as a flicker workaround. We render at panel
# native resolution and choose Newton's screen geometry as
# `panel / 2` so each Newton pixel is a 2×2 panel block — no panel-
# side scaling, no resampling. See `host_io::pi_fb::init`.
display_auto_detect=1

# Portrait rotation pairing for the `pi-fb-rot90` build feature —
# hardware-verified (90° clockwise), OFF by default because it
# depends on how the monitor is physically mounted. Uncomment
# together with gpu_mem=64 (above) and a `pi-fb-rot90` build ONLY.
# The firmware's physical-size readback comes back transposed while
# rotation is active; `display::fb::alloc_guest_surface` keys off
# that, so a rot90 build under an unrotated firmware fails loudly to
# the landscape fallback (the reverse mismatch still paints
# sideways). See docs/REAL_HW_BRINGUP.md "Portrait rotation".
#
# NOTE for a card currently running rotated: re-syncing this file
# (scripts/build-sd.sh with a mount arg) reverts it to landscape —
# re-apply gpu_mem=64 + this line after the sync.
#display_hdmi_rotate=1

# Force HDMI mode with audio support (vs DVI mode, which strips audio
# packets at the encoder side). Required for the VC4 HDMI MAI path
# in `src/audio/pi_hdmi.rs` to produce audible output; without it the
# encoder discards the audio info frames and subframes regardless of
# what we write into MAI_DATA. Safe to leave on with a pure-DVI sink
# attached — the receiver just ignores the audio packets.
hdmi_drive=2
