# Tolerated cross-layer edges, one per line:
#
#   <file-glob> <line-regex> # phase N — note
#
# check-layering.sh flags any forbidden import NOT matched here, and
# fails on stale entries (a listed edge that no longer exists must be
# deleted in the same change). This list may only shrink.
#
# Empty since refactor phase 7 (GuestOs hooks): the sanctioned edges —
# hv/mmio.rs → peripherals (closed-enum MMIO dispatch), hv/hooks.rs →
# newton (the ActiveGuest GuestOs seam), the peripherals/host service
# imports — are rules in check-layering.sh itself, not entries here.
